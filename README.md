# Deploy gitea docker image on ec2 with ALB via terraform

### Description

You would need to create the certificate ARN created using AWS Certificate Manager and supply it to var.certificate_arn in terraform
